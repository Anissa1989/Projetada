function getComputerChoice(chiffre) {
    if (chiffre === 1) {
        return "Papier"
    }
    if (chiffre === 2) {
        return "Caillou"
    }
    if (chiffre === 3) {
        return "Ciseaux"
    }  {return Math.floor(Math.random() * max);
}}
console.log(MgetComputerChoice());